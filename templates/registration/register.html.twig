
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HiParty - Inscription</title>
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@300;400;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/admin/assets/css/bootstrap.css">
    <link rel="stylesheet" href="/admin/assets/vendors/bootstrap-icons/bootstrap-icons.css">
    <link rel="stylesheet" href="/admin/assets/css/app.css">
    <link rel="stylesheet" href="/admin/assets/css/pages/auth.css">
</head>

<body>
    <div id="auth">
        
<div class="row h-100">
    <div class="col-lg-5 col-12">
        <div id="auth-left">
            <div class="auth-logo">
                <a href="/"><h2>HiParty</h2></a>
            </div>
            <h1 class="auth-title">Inscription</h1>
            <p class="auth-subtitle mb-5">Faites la fête dès ce soir.</p>

            

        

        <button type="submit" class="btn">Register</button>
   

        {{ form_start(registrationForm) }}
                <div class="form-group position-relative has-icon-left mb-4">
                    {{ form_widget(registrationForm.email, {'attr': {'class': 'form-control form-control-xl', 'placeholder': 'exemple@mail.fr'}}) }}
                    <div class="form-control-icon">
                        <i class="bi bi-envelope"></i>
                    </div>
                </div>
                <span class="text-danger"> {{ form_errors(registrationForm.email) }}</span>
                <div class="form-group position-relative has-icon-left mb-4">
                    {{ form_widget(registrationForm.name, {'attr': {'class': 'form-control form-control-xl', 'placeholder': 'Nom'}}) }}

                    <div class="form-control-icon">
                        <i class="bi bi-person"></i>
                    </div>
                </div>
                <span class="text-danger"> {{ form_errors(registrationForm.name) }}</span>
                <div class="form-group position-relative has-icon-left mb-4">
                    {{ form_widget(registrationForm.firstname, {'attr': {'class': 'form-control form-control-xl', 'placeholder': 'Prénom'}}) }}

                    <div class="form-control-icon">
                        <i class="bi bi-person"></i>
                    </div>
                </div>
                <span class="text-danger"> {{ form_errors(registrationForm.firstname) }}</span>
                <div class="form-group position-relative has-icon-left mb-4">
                    {{ form_widget(registrationForm.plainPassword.first, {'attr': {'class': 'form-control form-control-xl', 'placeholder': 'Mot de passe'}}) }}
                   
                     <div class="form-control-icon">
                        <i class="bi bi-shield-lock"></i>
                    </div>
                </div>
                <div class="form-group position-relative has-icon-left mb-4">
                    {{ form_widget(registrationForm.plainPassword.second, {'attr': {'class': 'form-control form-control-xl', 'placeholder': 'Répétez votre mot de passe'}}) }}

                    <div class="form-control-icon">
                        <i class="bi bi-shield-lock"></i>
                    </div>
                </div>
                <span class="text-danger"> {{ form_errors(registrationForm.plainPassword.first) }}</span>

                <label for="registration_form_agreeTerms">
                {{ form_widget(registrationForm.agreeTerms, {'attr': {'class': 'form-check-input form-check-xl'}}) }}
                <span class="text-danger"> {{ form_errors(registrationForm.agreeTerms) }}</span>
                    Accepter les CGU
                </label>
                <button class="btn btn-primary btn-block btn-lg shadow-lg mt-5">S'inscrire</button>
             {{ form_end(registrationForm) }}
            <div class="text-center mt-5 text-lg fs-4">
                <p class='text-gray-600'>Déjà inscrit ? <a href="{{ path('app_login') }}" class="font-bold">Connexion</a>.</p>
            </div>
        </div>
    </div>
    <div class="col-lg-7 d-none d-lg-block">
        <div id="auth-right">

        </div>
    </div>
</div>

    </div>
</body>

</html>

    

