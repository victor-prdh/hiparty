<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HiParty - Connexion</title>
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@300;400;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('admin/assets/css/bootstrap.css') }}">
    <link rel="stylesheet" href="{{ asset('admin/assets/vendors/bootstrap-icons/bootstrap-icons.css') }}">
    <link rel="stylesheet" href="{{ asset('admin/assets/css/app.css') }}">
    <link rel="stylesheet" href="{{ asset('admin/assets/css/pages/auth.css') }}">
</head>

<body>
    <div id="auth">
        
<div class="row">
    <div class="col-lg-5 col-12">
        <div id="auth-left">
            <div class="auth-logo">
                <a href="/"><h1>HiParty</h1></a>
            </div>
            <h1 class="auth-title">Connexion.</h1>
            <p class="auth-subtitle mb-5">Connectez-vous pour accéder à l'espace utilisateur.</p>

            {% if error %}
            <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        {% endif %}

            <form method="post">
                <div class="form-group position-relative has-icon-left mb-4">
                    <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control form-control-xl" required autofocus placeholder="mail@exemple.fr">
    
                    <div class="form-control-icon">
                        <i class="bi bi-person"></i>
                    </div>
                </div>
                <div class="form-group position-relative has-icon-left mb-4">
                    <input type="password" name="password" id="inputPassword" class="form-control form-control-xl" autocomplete="current-password" required placeholder="Mot de passe">
                    <div class="form-control-icon">
                        <i class="bi bi-shield-lock"></i>
                    </div>
                </div>
                <div class="form-check form-check-lg d-flex align-items-end">
                    <input type="checkbox" name="_remember_me" class="form-check-input me-2" id="_remember_me" >
                    <label class="form-check-label text-gray-600" for="_remember_me">
                        
                        Rester connecté
                    </label>
                </div>

                <input type="hidden" name="_csrf_token"
                value="{{ csrf_token('authenticate') }}"
         >

                <button class="btn btn-primary btn-block btn-lg shadow-lg mt-5" type="submit">Connexion</button>
            </form>
            <div class="text-center mt-5 text-lg fs-4">
                <p class="text-gray-600">Pas de compte ? <a href="{{ path('app_register') }}" class="font-bold">Inscrivez-vous</a>.</p>
                <p><a class="font-bold" href="{{ path('app_forgot_password_request') }}">Mot de passe oublié ?</a></p>
            </div>
        </div>
    </div>
    


    <div class="col-lg-7 d-none d-lg-block">
        <div id="auth-right">

        </div>
    </div>
</div>

    </div>
</body>

</html>
